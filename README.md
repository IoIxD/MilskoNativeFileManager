Drop in for [Milsko](https://github.com/pyrite-dev/milsko/)'s file picker widget that launches the OS's native file picker when it can, falling back to the old file picker if it can't.

See usage in `examples/example.c`
